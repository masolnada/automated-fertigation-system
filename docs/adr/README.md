# Architecture Decision Records — System-wide

Decisions that span more than one context. Each record is one decision in 1–3
sentences. Per-context decisions live under each context's own `docs/adr/` (or
`adr/` for the soil node); see [../../CONTEXT-MAP.md](../../CONTEXT-MAP.md).

| # | Decision |
|---|---|
| [0001](0001-mqtt-integration-backbone.md) | MQTT is the integration backbone between contexts |

If a decision is reversed, add a new ADR that supersedes the old one rather than
editing history.
