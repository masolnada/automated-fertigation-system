// Rendered to config.js by the container entrypoint (envsubst).
// For local development, copy to config.js and fill in real values.
window.FERTIGATION_CONFIG = {
  brokerUrl: "${MQTT_URL}",          // e.g. ws://10.0.20.20:9001 or wss://mqtt.example.com
  username: "${MQTT_USERNAME}",
  password: "${MQTT_PASSWORD}",
  prefix: "${MQTT_PREFIX}",          // e.g. kc868-a8
};
