import { variants } from "./theme/variants";
export function ValveSelect({ active, pending, pumpOn, onSelect }: { active: string; pending: string | null; pumpOn: boolean; onSelect(value: "" | "clean_water_valve" | "fertigation_valve"): void }) { const choices:[""|"clean_water_valve"|"fertigation_valve",string][]=[["clean_water_valve","Clean"],["","Closed"],["fertigation_valve","Fertigation"]]; return <><div className={variants.valve.select} role="group" aria-label="Valve selection">{choices.map(([value,label])=><button key={label} type="button" className={`${variants.valve.button}${active===value?` ${variants.valve.active}`:""}${pending===value?` ${variants.valve.pending}`:""}`} onClick={()=>onSelect(value)}>{label}</button>)}</div><p className={variants.valve.status} aria-live="polite">{pending===null?"":pumpOn?"Switching… both valves close for a moment, so the pump stops":"Switching… both valves close for a moment"}</p></>; }
