import { useEffect, useRef } from "react"; import { Button } from "./Button"; import { variants } from "./theme/variants";
type Props = { open: boolean; title: string; message: string; status?: string; danger?: boolean; pending?: boolean; confirmText: string; confirmDisabled?: boolean; onConfirm(): void; onClose(): void; opener?: HTMLElement | null };
export function Dialog({ open,title,message,status="",danger=false,pending=false,confirmText,confirmDisabled,onConfirm,onClose }: Props) { const cancelRef=useRef<HTMLButtonElement>(null),confirmRef=useRef<HTMLButtonElement>(null);useEffect(()=>{if(open)(pending?undefined:confirmRef.current)?.focus();},[open,pending]); if(!open)return null; const close=()=>{if(!pending)onClose();};const trap=(e:React.KeyboardEvent)=>{if(e.key==="Escape"){e.stopPropagation();close();return;}if(e.key!=="Tab"||pending)return;const first=cancelRef.current,last=confirmRef.current;if(e.shiftKey&&document.activeElement===first){e.preventDefault();last?.focus();}else if(!e.shiftKey&&document.activeElement===last){e.preventDefault();first?.focus();}};return <div className={variants.dialog.backdrop} onMouseDown={e=>{if(e.target===e.currentTarget)close();}} onKeyDown={trap}><section className={variants.dialog.panel} role="dialog" aria-modal="true" aria-labelledby="dialog-title" aria-describedby="dialog-message"><h2 id="dialog-title" className={variants.dialog.title}>{title}</h2><p id="dialog-message" className={variants.dialog.text}>{message}</p><p className={`${variants.dialog.status}${danger?` ${variants.dialog.danger}`:""}`} aria-live="polite">{status}</p><div className={variants.dialog.actions}><Button ref={cancelRef} onClick={close} disabled={pending}>Cancel</Button><Button ref={confirmRef} variant={danger?"danger":"default"} onClick={onConfirm} disabled={confirmDisabled||pending}>{confirmText}</Button></div></section></div>; }
