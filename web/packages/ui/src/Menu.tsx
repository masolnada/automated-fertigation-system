import { useEffect, useRef } from "react"; import { variants } from "./theme/variants";
export function Menu({ open, onToggle, onTrigger, children }: { open: boolean; onToggle(): void; onTrigger?(element: HTMLButtonElement): void; children: React.ReactNode }) { const trigger=useRef<HTMLButtonElement>(null); useEffect(()=>{const f=(e:MouseEvent)=>{if(open&&!trigger.current?.parentElement?.contains(e.target as Node)) onToggle();};document.addEventListener("click",f);return()=>document.removeEventListener("click",f);},[open,onToggle]); return <span className={variants.menu.wrap}><button ref={trigger} className={variants.menu.trigger} type="button" aria-haspopup="menu" aria-expanded={open} aria-controls="flow-menu" aria-label="Flow actions" onClick={e=>{e.stopPropagation();onTrigger?.(e.currentTarget);onToggle();}}>•••</button>{open&&<div id="flow-menu" className={variants.menu.panel} role="menu" aria-label="Flow actions">{children}</div>}</span>; }
