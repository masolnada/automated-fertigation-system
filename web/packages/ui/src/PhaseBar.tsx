import { variants } from "./theme/variants";
export function PhaseBar({ phases }: { phases: { id: string; name: string; minutes?: number }[] }) { return <div className={variants.phases} title="Sequence phases, to scale">{phases.map(p=>{const minutes=Number.isFinite(p.minutes)?p.minutes:undefined;const isFertigation=p.id.includes("fertigation");return <div key={p.id} className={`${variants.phase.base} ${isFertigation ? variants.phase.fertigation : variants.phase.normal}${typeof minutes==="number"&&minutes<8?` ${variants.phase.narrow}`:""}`} style={{flexGrow:Math.max(minutes ?? .4,.4)}} title={typeof minutes==="number"?`${p.name} — ${minutes} min`:undefined}><span className={variants.phase.label}>{p.name}</span><b className={variants.phase.value}>{minutes ?? "–"}</b></div>;})}</div>; }
