# Server-only MQTT broker and credentials (never sent to the browser).
MQTT_URL=mqtt://10.0.20.20:1883
MQTT_USERNAME=mosquitto
MQTT_PASSWORD=change-me
MQTT_PREFIX=kc868-a8
# Express server port (default 4000).
PORT=4000
