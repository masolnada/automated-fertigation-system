import { Card, CardTitle, variants } from "@hort/ui"; import type { Snapshot } from "@hort/mqtt";
const icon=<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><path d="M4 19.5A2.5 2.5 0 0 1 6.5 17H20"/><path d="M6.5 2H20v20H6.5A2.5 2.5 0 0 1 4 19.5v-15A2.5 2.5 0 0 1 6.5 2z"/></svg>;
export function Events({snapshot}:{snapshot:Snapshot}) {return <Card className="card-log"><CardTitle icon={icon}>Events</CardTitle><ul className={variants.events.list}>{snapshot.log.map((e,i)=><li key={`${e.time.getTime()}-${i}`} className={`${variants.events.row}${e.severity==="danger"?` ${variants.events.danger}`:""}`}><time className={variants.events.time}>{e.time.toLocaleTimeString()}</time>{e.message}</li>)}</ul></Card>; }
