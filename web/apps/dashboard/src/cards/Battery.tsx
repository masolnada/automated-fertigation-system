import { Badge, Card, CardTitle, Metric, variants } from "@hort/ui"; import { displayNumber, entities } from "@hort/mqtt"; import type { Snapshot } from "@hort/mqtt";
const icon=<svg viewBox="0 0 24 24" fill="none" stroke="currentColor" strokeWidth="2" strokeLinecap="round" strokeLinejoin="round"><rect x="2" y="7" width="16" height="10" rx="3"/><path d="M22 11v2"/><path d="m9 9-2 3h4l-2 3"/></svg>;
export function Battery({snapshot}:{snapshot:Snapshot}) { const ids=["battery_voltage","battery_current","battery_state_of_charge","battery_consumed_ah","battery_time_remaining"];const charged=snapshot.entities.battery_charged?.value==="ON";return <Card className="card-battery"><CardTitle icon={icon}>Battery <Badge state="on" hidden={!charged}>charged</Badge></CardTitle><dl className={variants.metric.list}>{ids.map(id=>{const e=entities[id]!;const v=snapshot.entities[id];return <Metric key={id} label={e.label} unit={e.unit} value={v?.known?displayNumber(v.value,id):"–"}/>;})}</dl></Card>; }
