# Plaintext secrets — only the age-encrypted secrets.enc.yaml is committed
/controller/secrets.yaml
/deploy/.env

# ESPHome build files
.esphome/

/.vscode

# Bun dashboard workspace
/web/node_modules/
/web/**/node_modules/
/web/**/dist/
/web/.env

# SQLite dev database (WAL/SHM sidecars included). DB_PATH defaults to
# ./data/hort.db, so the location depends on the server's working directory.
/web/apps/server/data/
/web/data/
